# WBS 拆分：修复 TUI 打开 Markdown 链接失败

任务：W-018
日期：2026-09-13

## T-01 共享链接目标解析

- 输入：资源值字符串，方案评审 PASS。
- 实现：新增 `internal/client/opener.Target`，覆盖 Markdown 链接、纯值、空值和不完整标记。
- 输出：解析后的平台打开目标或明确错误。
- 验收：解析单测通过，未引入 Markdown 渲染依赖。

## T-02 接入客户端打开路径

- 输入：T-01 helper。
- 实现：TUI `o` 和 `ttl open` 在平台命令前调用目标解析。
- 输出：Markdown 链接能交给系统打开器，既有状态机不变。
- 验收：TUI 打开成功/失败测试和 CLI 编译通过。

## T-03 回归验证

- 输入：T-01/T-02 代码和测试。
- 实现：运行 focused tests、CLI 回归、全量单测和静态检查。
- 输出：测试结果和验收证据。
- 依赖：T-01、T-02。
