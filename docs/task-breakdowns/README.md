# Task Breakdowns

这里保存已评审方案对应的 WBS。新建文件时复制 [`../templates/task-breakdown.md`](../templates/task-breakdown.md)，命名为 `YYYY-MM-DD-<slug>.md`，并链接需求、方案、方案评审和父工作项。

任务按功能闭环拆分，每个 `T-XX` 都要有输入、输出、依赖和验收标准。

WBS 只在技术方案通过评审后建立；`T-XX` 是父任务内部的执行单元，不自动成为看板上的独立项目任务。
