# Requirements

这里保存大功能的需求文档。新建文件时复制 [`../templates/requirement.md`](../templates/requirement.md)，命名为 `YYYY-MM-DD-<slug>.md`，并从 `WORK_ITEMS.md` 链接回来。

需求文档只描述目标、范围、规则、异常和验收标准，不放技术实现细节。

任务类型和阶段进度写在对应的 `WORK_ITEMS.md` 条目中；需求文档完成后才能进入技术设计。Bug 修复、文档和维护任务可以使用轻量流程，但要在任务的阶段清单中明确跳过的阶段。
