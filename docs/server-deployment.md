# 后端部署说明已迁移

自 W-021 起，`ttl-cli` 仓库只维护客户端，不再包含、构建或部署后端服务。

本文仅作为历史文档链接的兼容占位：W-010、W-017 和 W-019 的历史交付记录描述的是当时仓库内后端。新的后端部署、账号、认证、租户数据、升级和回滚说明应由独立后端工程维护。当前边界见 [`后端服务移出 ttl-cli 仓库`](decisions/2026-09-18-externalize-backend-service.md)。
