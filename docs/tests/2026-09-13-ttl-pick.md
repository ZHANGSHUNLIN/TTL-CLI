# W-013 测试计划

任务：W-013
状态：已执行

## Executed Coverage

- key/value/tag 大小写不敏感匹配和稳定候选顺序。
- 零条、单条、多条、重复命中、空 value、多行 value 和超长 key。
- 合法选择、取消、EOF、非法编号、越界和无 TTY 快速失败。
- stdout 只输出 value，提示和诊断进入 stderr；只读行为不修改资源。
- `--json`/`--non-interactive` 被拒绝，且机器模式校验发生在打开存储前。
- 黑盒覆盖单匹配管道输出、多匹配无 TTY 快速失败和不写入 history。

## Evidence

- `go test ./internal/client/cli`
- `go test ./...`
- `go vet ./...`
- `./scripts/cli-composability.sh`
- `./scripts/regression.sh`
- `git diff --check` 和脚本语法检查

上述检查于 2026-09-13 通过；owner 已于 2026-09-13 确认交互式 TTY 的合法编号、取消、空态、错误态和稳定排序。
