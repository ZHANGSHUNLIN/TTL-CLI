# W-007 测试计划与结果

任务：W-007
状态：自动检查和人工流程通过，待代码评审复审

## 已完成

- `go test ./internal/client/app ./internal/client/cli ./internal/client/tui ./integration_test/...`
- `./scripts/regression.sh /tmp/ttl-w007`
- `./scripts/verify.sh`
- `go test ./...`、`go vet ./...`
- 长列表分页、底部快捷键可见和 macOS/非 macOS 保存快捷键文案回归测试。
- PTY 空态、alternate screen、光标和 bracketed-paste 恢复检查。

## 待完成

- [x] 稳定文案本地化与五种 locale JSON 检查。
- [x] owner 已完成真实终端创建、搜索、详情、编辑、标签、打开、删除和窄屏流程（2026-09-13 确认）。
- [x] 详情页 `o` 打开当前 value；成功退出 TUI，失败保留详情页并显示错误。
- [x] 已修复代码评审中的文案本地化 `CONDITIONAL` 项，待复审确认。
