# TTL Pick Technical Design

日期：2026-09-13
任务：W-013
需求：`docs/requirements/2026-09-12-ttl-pick.md`
状态：draft

## Current Behavior

W-013 仍是候选能力，当前没有 `ttl pick` 实现或稳定接口。

## Proposed Ownership

待范围确认后由 `internal/client/cli` 负责命令契约，复用 `internal/client/app` 查询用例；交互选择与 stdout/stderr 契约需在方案评审中冻结。

## Open Design Decisions

- 是否承诺 `--print`。
- 空数据库错误类别。
- query 首尾空格语义。

## Gate

未完成上述决策前不得开始编码。
