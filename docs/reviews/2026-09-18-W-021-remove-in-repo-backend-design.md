# 方案评审：移除当前工程的后端实现

任务：W-021
结论：PASS

## 发现

无 `BLOCK` 或 `MEDIUM` 发现。

- `LOW`：删除内置 handler 后，本仓库不能证明独立后端的真实兼容性。方案已把边界限定为客户端 mock HTTP 契约；跨工程兼容验证作为独立后端接入后的后续工作，不阻塞本次源码收敛。
- `LOW`：历史任务与决策包含 `ttl-server` 路径。方案保留历史证据，并更新当前入口文档及决策状态说明，能够区分历史事实和当前结构。

## 覆盖确认

- 需求中的源码、构建、发布、测试和文档范围均有明确 owner。
- 删除操作不访问仓库外服务端数据，没有数据迁移或删除风险。
- cloud 客户端、同步编排和远程配置均保留，未把“移除后端实现”扩大成“移除远程能力”。
- 验证计划覆盖客户端构建、远程适配器、集成、race、vet、CLI 黑盒和 workflow 静态检查。

## 结论

方案可以拆分并实施。
