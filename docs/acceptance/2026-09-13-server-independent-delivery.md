# W-010 交付验收

日期：2026-09-13
任务：W-010
状态：验收通过，进入 commit 门禁

## 验收清单

- [x] CI 从 `./cmd/ttl` 构建 `ttl`，从 `./cmd/ttl-server` 构建 `ttl-server`。
- [ ] tag 发布独立的客户端和 Linux 服务端归档；服务端有 `amd64`、`arm64` 和
      SHA-256 校验文件。真实 tag 发布待人工执行。
- [x] 本地等价归档检查确认服务端交付不包含客户端、源码或数据文件；真实 tag 归档待核对。
- [x] 在没有仓库 checkout 或客户端二进制的干净 release 目录启动服务端。
- [x] `--listen`、`--port`、`--data-dir` 已校验并记录；默认监听为 loopback，不会意外暴露公网。
- [x] 就绪时 `/healthz` 返回 `200 {"status":"ok"}`，无需 API Key，且不返回用户或资源数据。
- [x] 用户文件损坏、数据目录路径不可用和端口占用在提供请求前失败，不删除数据；进程级失败边界已有测试证据。
- [x] SIGTERM/SIGINT 在限定时间内排空请求并关闭租户存储。
- [x] 参考部署使用非 root 用户、独立数据卷和进程管理器日志；远程测试机已确认非 root、独立
      数据目录和 systemd。
- [ ] 外部 TLS 终止：本次 loopback 测试环境未配置，保留为真实生产环境部署门禁。
- [x] 升级校验归档、备份数据、切换版本指针、重启并检查健康/API 就绪。
- [x] 升级失败可以恢复旧指针并继续提供旧数据；失败版本保留供诊断。
- [x] API、用户管理、租户隔离、客户端、集成、race 和静态检查通过 `./scripts/verify.sh`。
- [x] 本机直接使用 `ttl` 客户端访问 `http://10.99.48.2:8900`，完成远程 `add/get/update/del`
      链路验证，测试数据已清理。

## 待附证据

- [x] 本地构建、`./scripts/verify.sh`、交付冒烟、升级回滚和远程 systemd 部署记录。
- [ ] 真实 tag CI/release 运行链接、归档清单和 SHA-256 输出。
- [x] 负责人确认本次测试环境不需要客户端二进制或源码树，且测试输出未暴露 API Key 或资源值。

## 当前结果

本次测试环境验收通过，任务进入 commit 门禁。真实 tag 制品核对和生产环境外部 TLS 配置
仍是后续交付项；本地 commit 创建并补充提交证据后才能标记 `Done`。
