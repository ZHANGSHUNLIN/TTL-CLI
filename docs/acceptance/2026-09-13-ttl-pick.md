# W-013 交付验收

任务：W-013
状态：验收和代码评审完成，待本地提交

## Acceptance Criteria

- [x] 首期承诺 `ttl pick` 已由 W-013 确认。
- [x] v1 不提供 `--print`；空结果为 `not_found`；query 空格按原样匹配，均已写入需求和方案。
- [x] 无 TTY、stdout/stderr、退出码和只读保证有黑盒证据。
- [x] owner 已完成真实 TTY 下的合法编号、取消、空态、错误态和稳定排序验收（2026-09-13 确认）。

## 验收结论

自动检查、黑盒回归、owner 真实 TTY 验收和代码评审均通过；尚未创建本地 commit。
