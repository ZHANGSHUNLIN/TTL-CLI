# W-007 交付验收

任务：W-007
状态：人工验收完成，待代码评审复审

## 验收条件

- [x] 本地资源浏览、搜索、详情、创建/修改、标签和删除路径已实现。
- [x] CLI/TUI 共用 application service，未接入 cloud/sync。
- [x] 长列表按终端高度分页，列表内容不会遮挡底部快捷键和状态区。
- [x] 保存快捷键文案按操作系统显示：macOS 为 `Command+S`，其他系统为 `Ctrl+S`。
- [x] 详情页支持 `o` 打开当前资源；打开成功后自动退出 TUI，失败时保留详情页。
- [x] 稳定用户可见文案已完成本地化，覆盖五种 locale。
- [x] owner 已在真实终端完成完整 CRUD、标签、详情页打开、删除确认和窄屏验收（2026-09-13 确认）。

## 遗留风险

原代码评审为 `CONDITIONAL`；文案本地化整改已完成，需重新代码评审通过后才能进入 `Done`。
